import genDiff from '../src/genDiff.js';
import assert from 'power-assert';

const difference = genDiff();
const rightResult = 

test('get right difference', () => {
  expect(difference().toEqual());
})
